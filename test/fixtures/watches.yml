# == Schema Information
# Schema version: 20081021172636
#
# Table name: watches
#
#  user_id      :integer(4)      not null
#  idea_id      :integer(4)      not null
#  lock_version :integer(4)      default(0)
#  created_at   :datetime        not null
#

first_comment_watcher:
  user: quentin
  idea: first_idea